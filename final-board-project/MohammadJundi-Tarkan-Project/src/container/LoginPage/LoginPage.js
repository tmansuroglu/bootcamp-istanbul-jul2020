import React from "react";
import LoginForm from "../../components/LoginForm";

const LoginPage = () => {
    return (
        <LoginForm /> //read LoginForm component
    );
};

export default LoginPage;
